import styles from './review.module.css'
import Image from 'next/image'

export function ReviewCard({ reviewer, }: {
    reviewer: {
    
}; row: number, column: number, height: number }) {
    return (
        <div className={styles.reviewCard} style={{

        }}>
            
        </div> 
    )
}
